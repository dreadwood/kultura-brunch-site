{
  "name": "kultura-brunch-site",
  "description": "kultura-brunch site",
  "scripts": {
    "start": "parcel src/index.pug",
    "build": "parcel build src/index.pug",
    "editorconfig": "editorconfig-checker",
    "eslint": "eslint src/js/*/*.js",
    "gh-pages": "push-dir --dir=dist --branch=gh-pages",
    "sprite": "gulp sprite",
    "stylelint": "stylelint \"src/scss/**/*.scss\"",
    "test": "npm run editorconfig && npm run stylelint && npm run eslint"
  },
  "browserslist": [
    "last 4 versions",
    "not dead"
  ],
  "devDependencies": {
    "@parcel/transformer-pug": "^2.8.2",
    "@parcel/transformer-sass": "^2.8.2",
    "parcel": "^2.8.2",
    "sharp": "^0.31.3",
    "stylelint": "^14.16.0",
    "stylelint-config-htmlacademy": "^1.1.4",
    "stylelint-config-standard-scss": "^6.1.0",
    "stylelint-order": "^5.0.0"
  }
}
